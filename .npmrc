# Legacy npm configs - commented out for pnpm compatibility
# strict-peer-dependencies=false
# auto-install-peers=true
# shamefully-hoist=false
# resolution-mode=highest

# @aus-prop packages are local workspace packages, no registry needed
# @aus-prop:registry=https://npm.pkg.github.com/
# always-auth=true
# //npm.pkg.github.com/:_authToken=

# Pnpm workspace settings
# prefer-workspace-protocol=true

# Registry configuration
registry=https://registry.npmjs.org/
